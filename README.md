# Portwatch - Port & Process Monitor

Portwatch is a CLI tool built with Rust. It scans open ports on the localhost, displays the names of the processes using them, and supports a watch mode that automatically refreshes every N seconds.
